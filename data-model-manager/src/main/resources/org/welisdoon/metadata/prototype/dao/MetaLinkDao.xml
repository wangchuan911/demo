<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.welisdoon.metadata.prototype.dao.MetaLinkDao">
    <resultMap id="MetaLink" type="org.welisdoon.metadata.prototype.define.MetaLink">
        <result column="id" property="id"></result>
        <result column="type_id" property="typeId"></result>
        <result column="object_id" property="objectId"></result>
        <result column="value_id" property="valueId"></result>
        <result column="attribute_id" property="attributeId"></result>
        <result column="instance_id" property="instanceId"></result>
        <result column="link_id" property="linkId"></result>
        <result column="SEQUENCE" property="sequence"></result>
    </resultMap>
    <select id="list"
            parameterType="org.welisdoon.metadata.prototype.condition.MetaLinkCondition"
            resultMap="MetaLink">
        select * from MD_LINK
        <where>
            <if test="id!=null ">and id = #{id}</if>
            <if test="parentId!=null ">and parent_id = #{parentId}</if>
            <if test="data!=null">
                <if test="parentId==null and data.parentId!=null">and parent_id = #{data.parentId}</if>
                <if test="data.objectId!=null ">and object_id = #{data.objectId}</if>
                <if test="data.valueId!=null ">and value_id = #{data.valueId}</if>
                <if test="data.attributeId!=null ">and attribute_id = #{data.attributeId}</if>
                <if test="data.instanceId!=null ">and instance_id = #{data.instanceId}</if>
                <if test="data.typeId!=null">
                    and type_id in ( #{data.typeId,jdbcType=NUMERIC}
                    <foreach collection="@org.welisdoon.metadata.prototype.consts.LinkMetaType@getChildTypeId(data.typeId)" item="type_id">
                        ,#{type_id,jdbcType=NUMERIC}
                    </foreach>
                    )
                </if>
            </if>

        </where>

    </select>

    <select id="generate" parameterType="long" resultMap="MetaLink">
        select #{value} type_id from dual
    </select>

    <select id="get"
            parameterType="long"
            resultMap="MetaLink">
        select * from MD_LINK
        <where>
            and id=#{value}
        </where>

    </select>
    <insert id="add" parameterType="org.welisdoon.metadata.prototype.define.MetaLink" useGeneratedKeys="true" keyProperty="id">
        insert into MD_LINK(ID, TYPE_ID, `DESC`, PARENT_ID, OBJECT_ID, INSTANCE_ID, ATTRIBUTE_ID, SEQUENCE, VALUE_ID, LINK_ID)
        values(#{id}, #{typeId}, #{name},
               #{parentId,jdbcType=NUMERIC}, #{objectId,jdbcType=NUMERIC}, #{instanceId,jdbcType=NUMERIC},
               #{attributeId,jdbcType=NUMERIC}, #{sequence,jdbcType=NUMERIC}, #{valueId,jdbcType=NUMERIC},
               #{linkId,jdbcType=NUMERIC})
    </insert>
    <update id="put" parameterType="org.welisdoon.metadata.prototype.define.MetaLink">
        update MD_LINK set TYPE_ID=#{typeId}, `DESC`=#{name}, PARENT_ID=#{parentId,jdbcType=NUMERIC}, OBJECT_ID=#{objectId,jdbcType=NUMERIC},
                           INSTANCE_ID=#{instanceId,jdbcType=NUMERIC}, ATTRIBUTE_ID=#{attributeId,jdbcType=NUMERIC}, SEQUENCE=#{sequence,jdbcType=NUMERIC},
                           VALUE_ID=#{valueId,jdbcType=NUMERIC}, LINK_ID=#{linkId,jdbcType=NUMERIC}
        where ID=#{id}
    </update>
    <delete id="delete" parameterType="long">
        delete from MD_LINK where id=#{value}
    </delete>
</mapper>
