################### HTTP Configuration   ##########################
server:
  port: 443
  https:
    enable: true
    keyStore: /opt/wechat/thekeystore
    password: changeit
spring:
  profiles:
    active: [wechat,atomSrv]

################### Vert.x Configuration ##########################
vertx:
  worker:
    pool:
      size: 4
  workerVerticle:
    instances: 2
  clusterMode: hazelcast
  scanPath: pers.welisdoon.webserver
  dns:
    enable: false
    ip: 134.192.232.18,134.192.232.17
    maxQueries: 10
