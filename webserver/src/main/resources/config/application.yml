################### HTTP Configuration   ##########################
server:
  web:
    port: 443
    sslEnable: true
    sslKeyStore: /opt/wechat/thekeystore
    sslPassword: changeit
spring:
  profiles:
    active: [wechat,atomSrv,database]

################### Vert.x Configuration ##########################
vertx:
  worker:
    pool:
      size: 4
  workerVerticle:
    instances: 2
  clusterMode: hazelcast
  scanPath: pers.welisdoon.webserver
  dns:
    enable: false
    ip: 134.192.232.18,134.192.232.17
    maxQueries: 10
